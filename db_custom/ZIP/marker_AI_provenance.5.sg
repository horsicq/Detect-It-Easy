// Detect It Easy: C2PA carrier research rule for ZIP-derived formats

includeScript("_ai_provenance");
meta("marker", "C2PA manifest carrier");

function detect() {
    var oCarrier = aiDetectZipC2pa();

    if (oCarrier) {
        sVersion = oCarrier.version;
        sOptions = oCarrier.options;
        bDetected = true;
    }

    return result();
}
