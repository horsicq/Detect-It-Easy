// Detect It Easy: detection rule file

init("joiner", "Anskya Binder");

function detect() {
    if (PE.compareEP("BE........BB........33ED83EE04392E7411")) {
        sVersion = "1.1";
        bDetected = true;
    }

    return result();
}