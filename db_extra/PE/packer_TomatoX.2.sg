// Detect It Easy: detection rule file
// Author: KDSS-Research

meta("packer", "TomatoX");

function detect() {
    if (PE.isSectionNamePresent(".tomato")) {
        bDetected = true;
    }
    return result();
}