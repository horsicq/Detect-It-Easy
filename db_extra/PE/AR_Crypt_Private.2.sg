// DIE's signature file

init("cryptor", "AR Crypt Private");

function detect() {
    if (PE.compareEP("b8........5033c0c3") && PE.compareEP("b8########6033db33c9b8........3d........74")) {
        bDetected = true;
    }

    return result();
}