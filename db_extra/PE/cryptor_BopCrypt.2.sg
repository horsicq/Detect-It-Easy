// Detect It Easy: detection rule file

meta("cryptor", "BopCrypt");

function detect() {
    if (PE.compareEP("60BD........E8....0000")) {
        sVersion = "1.00";
        bDetected = true;
    }

    return result();
}