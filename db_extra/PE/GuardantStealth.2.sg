init("protector", "Guardant Stealth");

function detect() {
    if (PE.getNumberOfImports() == 1) {
        if (PE.getImportLibraryName(0) == "NOVEX32.dll") {
            bDetected = true;
        }
    }

    return result();
}