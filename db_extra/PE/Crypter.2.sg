// DIE's signature file

init("cryptor", "Crypter"); // ????????????

function detect() {
    if (PE.compareEP("68FF6424F06858585858FFD4508B40F205B095F6950F850181BBFF68")) {
        sVersion = "3.1";
        bDetected = true;
    }

    return result();
}