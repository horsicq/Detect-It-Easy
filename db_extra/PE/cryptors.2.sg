// Detect It Easy: detection rule file

init("cryptor", "unknwown cryptor");

function detect() {
    if (PE.compareEP("909090909090609ce8000000005dd14d..816d..........d709d8ab")) {
        sOptions = "by TMG";
        bDetected = true;
    }

    return result();
}