// Detect It Easy: detection rule file

init("protector", "cramble");

function detect() {
    if (MSDOS.compareEP("E9....609CFC..................01..012A..FF")) {
        sVersion = "0.2b3";
        bDetected = true;
    }

    return result();
}