// Detect It Easy: detection rule file

meta("protector", "SnoopStop");

function detect() {
    if (MSDOS.compareEP("..E9....EB..20......73..20....411A05")) {
        sVersion = "1.15";
        bDetected = true;
    }

    return result();
}