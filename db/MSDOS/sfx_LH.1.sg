// Detect It Easy: detection rule file
// Author: horsicq <horsicq@gmail.com>

meta("sfx", "LH");

function detect() {
    if (MSDOS.compare("'LH'27's SFX '", 0x24)) {
        bDetected = true;
    }

    return result();
}
