// Detect It Easy: detection rule file

meta("protector", "Aluwain");

function detect() {
    if (MSDOS.compareEP("8BEC1EE8....9D5E")) {
        sVersion = "8.09";
        bDetected = true;
    }

    return result();
}