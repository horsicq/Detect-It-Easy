// Detect It Easy: detection rule file
// Author: horsicq <horsicq@gmail.com>

meta("type", "NE");

function detect() {
    if (MSDOS.isNE()) {
        bDetected = true;
    }

    return result();
}
