// Detect It Easy: detection rule file
// Author: horsicq <horsicq@gmail.com>

meta("type", "LE");

function detect() {
    if (MSDOS.isLE()) {
        bDetected = true;
    }

    return result();
}
