// Detect It Easy: detection rule file
// Author: hypn0 <hypn0@mail.ru>

meta("protector", "XDOC");

function detect() {
    if (MSDOS.compareEP("2e8c16....eb00ea....ffff")) {
        sVersion = "1.20";
        bDetected = true;
    }

    return result();
}