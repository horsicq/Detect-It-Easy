// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

meta("runtime", "VROOM (Virtual Real-time Object-Oriented Memory)");

function detect() {
    if (MSDOS.compareOverlay("46424f56")) { // FBOV0
        bDetected = true;
    }

    return result();
}