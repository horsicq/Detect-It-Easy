// Detect It Easy: detection rule file

meta("protector", "Ciphator");

function detect() {
    if (MSDOS.compareEP("9393E8$$$$586A..0E68....CF")) {
        sVersion = "4.6";
        bDetected = true;
    }

    return result();
}