// Detect It Easy: UPP Packer detection for Python Bytecode
// https://github.com/KatzenTechnologies/UPP

init("packer", "UPP");

function detect() {
    if (PYC.isConstPresent("UPP!1.10")) {
        sVersion = "1.10";
        bDetected = true;
    }

    return result();
}
