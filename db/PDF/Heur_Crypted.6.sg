// Detect It Easy: detection rule file

init("format", "pdf");

function detect() {
    if (PDF.isValuesHexByKey("/U") && PDF.isValuesHexByKey("/O")) {
        _setResult("protection", "generic", "", "crypted data");
    }
}