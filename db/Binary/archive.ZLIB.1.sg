// Detect It Easy: detection rule file
// Author: LinXP

init("data");
includeScript("zlib");

function detect() {
    if (detect_zlib(Binary, 0)) {
        sName = "ZLIB data";
        bDetected = true;
    }

    return result();
}