// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>
// Used by Nintendo Wii - BackupMii/BootMii

init("format", "BackupMii");

function detect() {
    if (Binary.compare("'BackupMii v1'")) {
        sVersion = "v1"
        bDetected = true;
    }

    return result();
}
