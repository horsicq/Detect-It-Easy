// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

meta("text", "FST");

function detect() {
    if (Binary.compare("';Ligne de commentaires'")) {
        bDetected = true;
    }

    return result();
}
