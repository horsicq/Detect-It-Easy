// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// Used in Nina made by Makh Shevet
init("format", "UPI");

function detect() {
    if (Binary.compare("'UPI'")) {
        bDetected = true;
    }

    return result();
}
