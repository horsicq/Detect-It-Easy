// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>
// This Script supports following formats: XBE

init("format", "XBE");

function detect() {
    if (Binary.compare("'XBEH'")) {
        sVersion = "XBox Executable"
        bDetected = true;
    }

    return result();
}
