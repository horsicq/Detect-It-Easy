// DIE's signature file
// Author: Jason Hood <jadoxa@yahoo.com.au>

includeScript("Cab");

function detect(bShowType,bShowVersion,bShowOptions)
{
    detect_Cab(0,Binary.getSize(),bShowOptions);
    return result(bShowType,bShowVersion,bShowOptions);
}
