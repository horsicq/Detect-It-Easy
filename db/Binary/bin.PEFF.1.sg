// Detect It Easy: detection rule file

init("format", "Preferred Executable Format");

function detect() {
    if (Binary.compare("'Joy!peff'")) {
        bDetected = true;
    }

    return result();
}