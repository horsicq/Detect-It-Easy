// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://en.wikipedia.org/wiki/Uplink_(video_game)
meta("archive", "RedShirt");

function detect() {
    if (Binary.compare("'REDSHIRT'00")) {
        bDetected = true;
    }

    return result();
}