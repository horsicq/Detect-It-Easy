// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

meta("audio", "Vorbis");

function detect() {
    if (Binary.compare("'OggS'")) {
        sVersion = "OGG";
        bDetected = true;
    }

    return result();
}
