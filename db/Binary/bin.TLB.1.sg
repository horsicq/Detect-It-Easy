// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

init("format", "Microsoft Type Library");

function detect() {
    if (Binary.compare("'MSFT'")) {
        bDetected = true;
    }

    return result();
}
