// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

meta("format", "gBurner Image (.GBI)");

function detect() {
    if (Binary.compare("'GBI'")) {
        bDetected = true;
    }

    return result();
}
