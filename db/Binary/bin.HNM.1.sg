// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

init("format", "HNM");

function detect() {
    if (Binary.compare("'HNM6'")) {
        bDetected = true;
    }

    return result();
}
