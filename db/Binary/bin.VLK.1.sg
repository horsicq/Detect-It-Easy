// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

init("format", "VLK");

function detect() {
    if (Binary.compare("'@VLK@2.5.0'")) {
        sVersion = "2.5.0";
        bDetected = true;
    }

    return result();
}
