// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>
// This Script supports following formats: ICNS

init("image", "Apple Icon Image");

function detect() {
    if (Binary.compare("'icns'")) {
        bDetected = true;
    }

    return result();
}
