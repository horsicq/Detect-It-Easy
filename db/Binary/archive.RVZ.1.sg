// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>
meta("archive", "RVZ");

function detect() {
    if (Binary.compare("'RVZ'")) {
        var gameID = Binary.getString(88, 6).trim();

        if (gameID) {
            sOptions = "GameID: " + gameID;
            bDetected = true;
        }
    }

    return result();
}
