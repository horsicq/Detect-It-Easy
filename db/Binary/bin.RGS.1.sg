// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>
// Used by InstallShield Professional

init("format", "InstallShield (Script for Windows Registry)");

function detect() {
    if (Binary.compare("'HKCR'")) {
        bDetected = true;
    }

    return result();
}
