// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

meta("audio", "PAT");

function detect() {
    if (Binary.compare("'PT'")) {
        sVersion = "Audio";
        bDetected = true;
    }

    return result();
}
