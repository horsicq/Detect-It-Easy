// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

init("format", "TRE");

function detect() {
    if (Binary.compare("'ZblT'")) {
        bDetected = true;
    }

    return result();
}
