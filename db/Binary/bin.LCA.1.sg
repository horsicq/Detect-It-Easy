// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

meta("format", "LCA");

function detect() {
    if (Binary.compare("4C 45 47 4F")) {
        sVersion = "VRT v5.10";
        bDetected = true;
    }

    return result();
}
