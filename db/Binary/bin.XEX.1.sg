// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>
// This Script supports following formats: XEX

init("format", "XEX");

function detect() {
    if (Binary.compare("'XEX2'")) {
        sVersion = "Xbox Executable File"
        bDetected = true;
    }

    return result();
}
