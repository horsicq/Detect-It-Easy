// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

init("text", "AS");

function detect() {
    if (Binary.compare("'Act;'")) {
        bDetected = true
    }

    return result();
}
