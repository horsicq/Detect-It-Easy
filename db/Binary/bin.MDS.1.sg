// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>
// This Script supports following formats: MDS

init("format", "MDS");

function detect() {
    if (Binary.compare("'MEDIA DESCRIPTOR'")) {
        sVersion = "Media Descriptor Sheet"
        bDetected = true;
    }

    return result();
}
