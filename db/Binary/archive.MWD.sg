// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>
// Used by Frogger

init("archive", "MWD");

function detect() {
    if (Binary.compare("44 41 57 4D")) {
        sName = "Medievil WAD";
        bDetected = true;
    }

    return result();
}