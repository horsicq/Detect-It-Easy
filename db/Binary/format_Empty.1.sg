// Detect It Easy: detection rule file
// Author: horsicq <horsicq@gmail.com>

meta("format", "Empty file");

function detect() {
    if (Binary.getSize() == 0) {
        bDetected = true;
    }

    return result();
}
