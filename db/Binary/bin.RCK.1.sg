// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

meta("format", "RCK");

function detect() {
    if (Binary.compare("'RKET'")) {
        bDetected = true;
    }

    return result();
}
