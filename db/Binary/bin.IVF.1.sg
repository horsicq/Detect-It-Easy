// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

meta("format", "IVF");

function detect() {
    if (Binary.compare("'DKIF'")) {
        bDetected = true;
    }

    return result();
}
