// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://github.com/jamie/ciso
meta("format", "Compressed ISO file (.CISO)");

function detect() {
    if (Binary.compare("'CISO'0000000000")) {
        bDetected = true;
    }

    return result();
}