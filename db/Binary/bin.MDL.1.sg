// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://developer.valvesoftware.com/wiki/MDL
init("format", "MDL");

function detect() {
    if (Binary.compare("'IDST1'")) {
        bDetected = true;
    }

    return result();
}
