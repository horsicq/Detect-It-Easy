// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>
// This Script supports following formats: FNX
// Used by Iron Willy

init("format", "FNX");

function detect() {
    if (Binary.compare("'adgames'")) {
        sVersion = "Font [1996]"
        bDetected = true;
    }

    return result();
}
