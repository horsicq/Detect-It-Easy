// Detect It Easy: DiE-JS framework file
// Don't change anything unless you're sure about what you're doing

var File = Binary;
var X = Binary;


var Binary_getSize = Binary.getSize,
    Binary_fileSize = Binary_getSize();

/**
 * Get the cached file size.
 * @returns {Number}
 */
Binary.getSize = function () {
    return Binary_fileSize;
}

includeScript("read");