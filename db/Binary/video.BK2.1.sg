// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

meta("format", "BinkMovie");

function detect() {
    if (Binary.compare("'KB2'")) {
        sVersion = "v2.0";
        bDetected = true;
    }

    return result();
}
