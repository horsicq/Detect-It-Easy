// Detect It Easy: detection rule file
// Author: LinXP
// Status: WIP

meta("archive", "FreeARC Archive (.ARC)");

function detect() {
    if (Binary.getSize() >= 9) {

        if (Binary.compare("417243010000")) {
            bDetected = true;
        }
    }

    return result();
}