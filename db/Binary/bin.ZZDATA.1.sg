// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

init("format", "ZZDATA");

function detect() {
    if (Binary.compare("'DIRINFO'")) {
        sVersion = "CDi";
        bDetected = true;
    }

    return result();
}