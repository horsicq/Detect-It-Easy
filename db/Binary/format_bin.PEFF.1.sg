// Detect It Easy: detection rule file

meta("format", "Preferred Executable Format");

function detect() {
    if (Binary.compare("'Joy!peff'")) {
        bDetected = true;
    }

    return result();
}