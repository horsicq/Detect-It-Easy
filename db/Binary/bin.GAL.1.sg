// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://graphicsgale.com/
meta("format", "Gale (.GAL)");

function detect() {
    if (Binary.compare("'Gale'")) {
        sVersion = File.cleanString(Binary.getString(0x0004));
        bDetected = true;
    }

    return result();
}