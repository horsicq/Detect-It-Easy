// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>
// This Script supports following formats: TFM

init("graphics", "TFM");

function detect() {
    if (Binary.compare("'tfm'")) {
        bDetected = true;
    }

    return result();
}
