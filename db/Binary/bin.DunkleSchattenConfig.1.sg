// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

meta("format", "Dunkle Schatten Config");

function detect() {
    if (Binary.compare("'DSchCFG'")) {
        bDetected = true;
    }

    return result();
}
