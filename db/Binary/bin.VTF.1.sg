// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

init("format", "VTF");

function detect() {
    if (Binary.compare("'VTF'")) {
        bDetected = true;
    }

    return result();
}
