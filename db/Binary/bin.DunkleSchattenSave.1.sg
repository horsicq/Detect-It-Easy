// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

meta("format", "Dunkle Schatten Save");

function detect() {
    if (Binary.compare("'DSchSAV'")) {
        bDetected = true;
    }

    return result();
}
