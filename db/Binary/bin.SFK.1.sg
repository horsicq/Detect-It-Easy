// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

init("format", "SFK");

function detect() {
    if (Binary.compare("'SFPK'")) {
        bDetected = true;
    }

    return result();
}
