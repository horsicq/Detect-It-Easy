// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

meta("format", "HSM");

function detect() {
    if (Binary.compare("'1LMB'")) {
        bDetected = true;
    }

    return result();
}
