// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>
// This Script supports following formats: BUP / IFO

init("format", "Backup file");

function detect() {
    if (Binary.compare("'DVDVIDEO-VTS'")) {
        bDetected = true;
    }

    return result();
}
