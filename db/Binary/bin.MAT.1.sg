// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

meta("format", "MAT");

function detect() {
    if (Binary.compare("'MATF'")) {
        bDetected = true;
    }

    return result();
}
