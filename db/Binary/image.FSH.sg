// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

meta("image", "FSH");

function detect() {
    if (Binary.compare("'SHPI'")) {
        bDetected = true;
    }

    return result();
}
