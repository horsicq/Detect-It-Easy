// Detect It Easy: detection rule file
// Author: Jason Hood <jadoxa@yahoo.com.au>

meta("script", "Shell");

includeScript("shell-script");

function detect() {
    if (isInterpreter("sh")) {
        bDetected = true;
    }

    return result();
}