// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

init("format", "MB2");

function detect() {
    if (Binary.compare("'BNAM'")) {
        bDetected = true;
    }

    return result();
}
