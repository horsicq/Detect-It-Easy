// Detect It Easy: detection rule file
// Author: horsicq <horsicq@gmail.com>

meta("library", "Cocoa");

function detect() {
    if (MACH.isLibraryPresent("Cocoa")) {
        bDetected = true;
    }

    return result();
}
