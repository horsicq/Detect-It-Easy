// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://cdburnerxp.se/
meta("tool", "CDBurnerXP");

function detect() {
    if (Binary.compare("'CDBURNERXP PRO 3'", 0x0000823e)) {
        sVersion = "Pro 3";
        bDetected = true;
    }

    return result();
}