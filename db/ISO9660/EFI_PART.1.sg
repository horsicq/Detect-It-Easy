// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://en.wikipedia.org/wiki/EFI_system_partition
meta("partition", "EFI PART");

function detect() {
    if (Binary.compare("'EFI PART'", 0x00000200)) {
        bDetected = true;
    }

    return result();
}