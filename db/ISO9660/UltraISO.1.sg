// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://www.ultraiso.com/
meta("iso", "UltraISO");

function detect() {
    if (Binary.compare("'9.2.0.2536'", 0x0000837c)) {
        sVersion = "9.2.0.2536";
        bDetected = true;
    }

    return result();
}