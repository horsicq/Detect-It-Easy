// Detect It Easy: detection rule file

init("operation system", "OS/2");

function detect() {
    if (LX.isVerbose()) {
        sName = LX.getOperationSystemName();
        sVersion = LX.getOperationSystemVersion();
        sOptions = LX.getOperationSystemOptions();

        bDetected = true;
    }

    return result();
}