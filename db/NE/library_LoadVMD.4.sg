// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

meta("library", "LoadVMD");

function detect() {
    if (NE.compare("'LoadVMD for Windows'", 0x00000407)) {
        bDetected = true;
    }

    return result();
}