// Detect It Easy: detection rule file

meta("language", "JavaScript");

function detect() {
    if (NPM.isArchiveRecordPresentExp("(.*?).js")) {
        bDetected = true;
    }

    return result();
}