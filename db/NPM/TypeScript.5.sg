// Detect It Easy: detection rule file

meta("language", "TypeScript");

function detect() {
    if (NPM.isArchiveRecordPresentExp("(.*?).ts")) {
        bDetected = true;
    }

    return result();
}