// Detect It Easy: DiE-JS framework file
// Don't change anything unless you're sure about what you're doing

var File = ZIP;
var X = ZIP;


var ZIP_isArchiveRecordPresent = ZIP.isArchiveRecordPresent,
    ZIP_archiveRecordSearchResults = {};

/**
 * Check if an archive record is present and cache the result.
 * @returns {Boolean}
 */
ZIP.isArchiveRecordPresent = function (sRecordName) {
    var sCacheKey = "$" + sRecordName;

    if (ZIP_archiveRecordSearchResults[sCacheKey] === undefined) {
        ZIP_archiveRecordSearchResults[sCacheKey] = ZIP_isArchiveRecordPresent(sRecordName);
    }

    return ZIP_archiveRecordSearchResults[sCacheKey];
}