// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://hex-rays.com/
meta("library", "IDA");

function detect() {
    if (PE.isLibraryPresent("ida.dll")) {
        bDetected = true;
    }

    return result();
}