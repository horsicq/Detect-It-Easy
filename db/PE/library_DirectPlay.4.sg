// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://en.wikipedia.org/wiki/DirectPlay
meta("library", "DirectPlay");

function detect() {
    if (PE.isLibraryPresentExp(/^dplay(x|)/i)) {
        bDetected = true;
    }

    return result();
}