// Detect It Easy: detection rule file

init("protector", "Yano");

function detect() {
    if (PE.isNetObjectPresent("YanoAttribute")) {
        sVersion = "1.X";
        bDetected = true;
    }

    return result();
}