// Detect It Easy: detection rule file

meta("protector", "Yano");

function detect() {
    if (PE.isNetObjectPresent("YanoAttribute")) {
        sVersion = "1.X";
        bDetected = true;
    }

    return result();
}