// Detect It Easy: detection rule file
// Made by KDSS-Research

meta("packer", "Squishy");

function detect() {
    if (PE.isSectionNamePresent("logicoma")) {
        bDetected = true;
    }

    return result();
}