// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// http://wiki.maniac-mansion-mania.de/wiki/SCUMM
meta("game engine", "Sputm (SCUMM)");

function detect() {
    if (PE.getVersionStringInfo("ProductName") == "SPUTM Engine") {
        bDetected = true;
    }

    return result();
}