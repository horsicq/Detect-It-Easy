// Detect It Easy: detection rule file

init("protector", "UG2002 Cruncher");

function detect() {
    if (PE.compareEP("60E8........5D81ED........E80D................................58")) {
        sVersion = "0.3b3";
        bDetected = true;
    }

    return result();
}