// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://windows10dll.nirsoft.net/shfolder_dll.html
meta("library", "ShFolder");

function detect() {
    if (PE.isLibraryPresent("SHFOLDER.dll")) {
        bDetected = true;
    }

    return result();
}