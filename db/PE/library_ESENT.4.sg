// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://windows10dll.nirsoft.net/esent_dll.html
meta("library", "Extensible Storage Engine (ESE)");

function detect() {
    if (PE.isLibraryPresent("ESENT.dll")) {
        bDetected = true;
    }

    return result();
}