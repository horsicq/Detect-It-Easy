// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

init("metadata", "Volatile");

function detect() {
    if (PE.isSectionNamePresent(".voltbl")) {
        bDetected = true;
    }

    return result();
}
