// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://xpdll.nirsoft.net/normaliz_dll.html
meta("library", "Unicode Normalization");

function detect() {
    if (PE.isLibraryPresent("Normaliz.dll")) {
        bDetected = true;
    }

    return result();
}