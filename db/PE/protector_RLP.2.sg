// Detect It Easy: detection rule file

meta("protector", "RLP");

function detect() {
    if (PE.isSectionNamePresent(".rlp")) {
        sVersion = "0.7.3b";
        bDetected = true;
    }

    return result();
}