// DIE's signature file
// Author: DosX; Telegram: @DosX_dev

function detect() {
    if (PE.isNET() && PE.isNETStringPresent("Guna.UI2") && PE.isNETStringPresent("Guna.UI2.WinForms"))
        _setResult("library", "Guna UI", "v2.X", "");
}