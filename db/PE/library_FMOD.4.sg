// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://www.fmod.com/
meta("library", "FMOD");

function detect() {
    if (PE.isLibraryPresentExp(/^fmod/)) {
        bDetected = true;
    }

    return result();
}