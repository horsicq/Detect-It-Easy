// Detect It Easy: detection rule file

// Author: DosX
// GitHub: https://github.com/DosX-dev
// Website: https://dosx.su
// Telegram: @DosX_dev

// https://github.com/d35ha/xObf
meta("protector", "xObf");

function detect() {
    var xObfLastSection = PE.section[PE.nLastSection];

    if (xObfLastSection && xObfLastSection.Name === ".xObf" && xObfLastSection.IsReadWriteExecute) {
        bDetected = true;
    }

    return result();
}