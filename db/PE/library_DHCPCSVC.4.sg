// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://windows10dll.nirsoft.net/dhcpcsvc_dll.html
meta("library", "DHCP Client Service");

function detect() {
    if (PE.isLibraryPresent("dhcpcsvc.dll")) {
        bDetected = true;
    }

    return result();
}