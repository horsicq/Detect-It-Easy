// Detect It Easy: detection rule file

init("cryptor", "INCrypter");

function detect() {
    if (PE.compareEP("6064A1300000008B400C8B400C8D5820C70300000000E8000000005D81ED")) {
        sVersion = "0.3";
        bDetected = true;
    }

    return result();
}