// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://en.wikipedia.org/wiki/WinG
meta("library", "WinG");

function detect() {
    if (PE.isLibraryPresent("WING32.DLL")) {
        bDetected = true;
    }

    return result();
}