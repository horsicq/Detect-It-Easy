// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://battle.net
meta("library", "Battlenet");

function detect() {
    if (PE.isExportFunctionPresent("GetBattlenetAllocator")) {
        bDetected = true;
    }

    return result();
}