// Detect It Easy: detection rule file

init("cryptor", "ChainskiCrypter");

function detect() {
    if (PE.isNetObjectPresent("Chainski")) {
        bDetected = true;
    }

    return result();
}