// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://github.com/sezero/mikmod
meta("library", "Mikmod");

function detect() {
    if (PE.isLibraryPresentExp(/^libmikmod/)) {
        bDetected = true;
    }

    return result();
}