// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://www.unrealengine.com/
meta("game engine", "Unreal");

function detect() {
    if (PE.getVersionStringInfo("ProductName") === "Unreal Engine") {
        bDetected = true;
    }

    return result();
}