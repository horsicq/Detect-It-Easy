// Detect It Easy: detection rule file

// Author: DosX
// GitHub: https://github.com/DosX-dev
// Website: https://dosx.su
// Telegram: @DosX_dev

// https://github.com/NLog/NLog
meta("library", "NLog");

function detect() {
    if (PE.isNetObjectPresent("LogManager") && PE.isNetObjectPresent("Logger")) {
        bDetected = true;
    }

    return result();
}