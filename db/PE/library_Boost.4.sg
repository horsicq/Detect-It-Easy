// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://www.boost.org/
meta("library", "Boost");

function detect() {
    if (PE.isLibraryPresentExp(/libboost/i)) {
        bDetected = true;
    }

    return result();
}