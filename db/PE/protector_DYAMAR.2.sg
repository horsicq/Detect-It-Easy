// Detect It Easy: detection rule file

meta("protector", "DYAMAR");

function detect() {
    if (PE.isSectionNamePresentExp(/^\.dyamar[CD]$/)) {
        sVersion = "1.3.5";
        bDetected = true;
    }

    return result();
}