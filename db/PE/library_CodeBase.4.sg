// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://github.com/MPSystemsServices/CodeBase-for-DBF
meta("library", "CodeBase");

function detect() {
    if (PE.isExportFunctionPresentExpFastAnchor("l4linkst", /l4linkSt/)) {
        sVersion = "4.0";
        sOptions = "static"; // EAT
        bDetected = true;
    }

    return result();
}