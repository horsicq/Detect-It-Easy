// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://kaimi.io/en/2024/07/analyzing-xfs-api-calls/
meta("library", "MSXFS");

function detect() {
    if (PE.isLibraryPresent("MSXFS.dll")) {
        bDetected = true;
    }

    return result();
}