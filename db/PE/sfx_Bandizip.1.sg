// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://www.bandisoft.com/bandizip/
meta("sfx", "Bandizip");

function detect() {
    if (PE.compareOverlay("'BANDIZIPSFX'00'PK'0304")) {
        bDetected = true;
    }

    return result();
}