// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://github.com/leethomason/tinyxml2
meta("library", "TinyXml");

function detect() {
    if (PE.isLibraryPresent("tinyxml.dll")) {
        bDetected = true;
    }

    return result();
}