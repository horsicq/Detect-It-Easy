// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// http://www.libpng.org/
meta("library", "libpng");

function detect() {
    if (PE.isLibraryPresentExp(/^libpng*/)) {
        bDetected = true;
    }

    return result();
}