// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://learn.microsoft.com/en-us/office/client-developer/excel/creating-xlls
meta("library", "Microsoft Excel Callback");

function detect() {
    if (PE.isLibraryPresent("XLCALL32.dll")) {
        bDetected = true;
    } else if (PE.isExportFunctionPresent("xlAutoOpen")) { // https://learn.microsoft.com/en-us/office/client-developer/excel/xlautoopen
        sOptions = "static"; // EAT
        bDetected = true;
    }

    return result();
}