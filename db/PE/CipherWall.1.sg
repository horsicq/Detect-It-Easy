// Detect It Easy: detection rule file

init("sfx", "CipherWall");

function detect() {
    if (PE.compareEP("9061BE........8DBE0000FEFFC787C0200200")) {
        bDetected = true;
        sVersion = "1.5";
    }

    return result();
}