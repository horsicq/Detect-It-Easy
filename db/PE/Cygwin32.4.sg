// Detect It Easy: detection rule file format

init("compiler", "Cygwin32");

function detect() {
    if (PE.compareEP("5589E583EC04833D")) {
        bDetected = true;
    }

    return result();
}
