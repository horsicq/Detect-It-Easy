// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://www.neteasegames.com/
meta("protection", "NetEase");

function detect() {
    if (PE.isLibraryPresent("NEP2.dll")) {
        sOptions = "Protect II";
        bDetected = true;
    }

    return result();
}