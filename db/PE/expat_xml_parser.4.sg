// Detect It Easy: detection rule file

// Author: DosX
// E-Mail: collab@kay-software.ru
// GitHub: https://github.com/DosX-dev
// Telegram: @DosX_dev

// https://libexpat.github.io/
init("library", "Expat XML Parser");

function detect() {
    if (PE.isLibraryPresentExp(/^libexpat/i)) {
        bDetected = true;
    }

    return result();
}