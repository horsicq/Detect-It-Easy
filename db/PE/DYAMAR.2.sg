// Detect It Easy: detection rule file

init("protector", "DYAMAR");

function detect() {
    if (PE.section[".dyamarC"] && PE.section[".dyamarD"]) {
        sVersion = "1.3.5";
        bDetected = true;
    }

    return result();
}