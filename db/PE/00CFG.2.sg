// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

init("config", "Control Flow Guard");

function detect() {
    if (PE.isSectionNamePresent(".00cfg")) {
        bDetected = true;
    }

    return result();
}
