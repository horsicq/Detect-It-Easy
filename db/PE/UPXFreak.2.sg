// Detect It Easy: detection rule file

init("tool", "UPXFreak");

function detect() {
    if (PE.compareEP("BE........83C601FFE60000")) {
        sVersion = "0.1";
        bDetected = true;
    }

    return result();
}