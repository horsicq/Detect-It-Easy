// Detect It Easy: detection rule file
// Author: Vito <@ScalletaZ>

init("protector", "Virbox");

function detect() {
    if (PE.compare("'SENS'", 40)) {
        bDetected = true;
    }

    return result();
}