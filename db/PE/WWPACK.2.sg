// Detect It Easy: detection rule file

init("packer", "WWPACK");

function detect() {
    if (PE.compareEP("53558BE833DBEB60'\r\n\r\nWWPack32'")) {
        bDetected = true;
    }

    return result();
}