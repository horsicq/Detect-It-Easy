// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://www.igdb.com/game_engines/void-engine
meta("game engine", "Void");

function detect() {
    if (PE.getVersionStringInfo("ProductName") == "VoidEngine") {
        bDetected = true;
    }

    return result();
}