// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://github.com/pythernel/Shroud
meta("cryptor", "Shroud");

function detect() {
    if (PE.compareOverlay("'SHROUD1.0'adde0000efbe")) {
        sVersion = "1.0";
        bDetected = true;
    }

    return result();
}