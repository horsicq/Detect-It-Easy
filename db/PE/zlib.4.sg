// Author: DosX
// E-Mail: collab@kay-software.ru
// GitHub: https://github.com/DosX-dev
// Telegram: @DosX_dev

init("library", "zlib");

function detect() {
    if (PE.isLibraryPresentExp(/^zlib*/)) {
        bDetected = true;
    }

    return result();
}