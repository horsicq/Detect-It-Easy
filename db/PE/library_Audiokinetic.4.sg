// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://www.audiokinetic.com/en/
meta("library", "Audiokinetic");

function detect() {
    if (PE.isExportFunctionPresent("AkTlsAllocateSlot")) {
        bDetected = true;
    }

    return result();
}