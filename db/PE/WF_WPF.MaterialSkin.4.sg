// Detect It Easy: detection rule file

// Author: DosX
// E-Mail: collab@kay-software.ru
// GitHub: https://github.com/DosX-dev
// Telegram: @DosX_dev

init("library", "MaterialSkin UI");

function detect() {
    if (PE.isNet() && PE.isNetObjectPresent("MaterialSkin")) {
        bDetected = true;
    }

    return result();
}