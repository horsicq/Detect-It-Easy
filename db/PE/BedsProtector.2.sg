// Detect It Easy: detection rule file

// Author: DosX
// E-Mail: collab@kay-software.ru
// GitHub: https://github.com/DosX-dev
// Telegram: @DosX_dev

init("protector", "Beds Protector");

function detect() {
    if (PE.isNet() && PE.getNetAssemblyName().indexOf("вє∂ѕ ρяσтє¢тσя") == 0) {
        sVersion = "ConfuserEx modded";
        bDetected = true;
    }

    return result();
}