// Detect It Easy: detection rule file

meta("packer", "PeX");

function detect() {
    if (PE.compareEP("E9$$$$$$$$60e8$$$$$$$$83c404e8")) {
        sVersion = "0.99";
        bDetected = true;
    }

    return result();
}