// Detect It Easy: detection rule file
// Author: Jason Hood <jadoxa@yahoo.com.au>

init("camera", "");

function detect() {
    sName = Jpeg.getExifCameraName();

    if (sName != "") {
        bDetected = true;
    }

    return result();
}