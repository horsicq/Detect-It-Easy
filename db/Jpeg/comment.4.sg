// Detect It Easy: detection rule file
// Author: Jason Hood <jadoxa@yahoo.com.au>

init("image", "comment");

function detect() {
    sOptions = Jpeg.getComment();

    if (sOptions != "") {
        bDetected = true;
    }

    return result();
}