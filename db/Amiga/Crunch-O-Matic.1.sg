// Detect It Easy: detection rule file
// Author: hypn0 <hypn0@mail.ru>

init("cruncher", "Crunch-O-Matic");

function detect() {
    if (Amiga.compareEP("48e7fffe267a....d7cbd7cb588b2c78....224b")) {
        sVersion = "1.0";
        sOptions = "Execute";
        bDetected = true;
    }

    return result();
}