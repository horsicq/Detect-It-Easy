// Detect It Easy: detection rule file
// Author: hypn0 <hypn0@mail.ru>

init("sfx", "LHASFX");

function detect() {
    if (Amiga.compareEP("6000....0000abcd'SFX!'")) {
        sOptions = "temp detect";
        bDetected = true;
    }

    return result();
}