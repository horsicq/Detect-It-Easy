// Detect It Easy: detection rule file
// Author: hypn0 <hypn0@mail.ru>

init("library", "library");

function detect() {
    if (Amiga.compareEP("70..4e75")) {
        bDetected = true;
    }

    return result();
}