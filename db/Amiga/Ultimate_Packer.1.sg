// Detect It Easy: detection rule file
// Author: hypn0 <hypn0@mail.ru>

init("cruncher", "Ultimate Packer");

function detect() {
    if (Amiga.compareEP("600000220000abcd00000000")) {
        sVersion = "1.1";
        sOptions = "temp detect";
        bDetected = true;
    }

    return result();
}