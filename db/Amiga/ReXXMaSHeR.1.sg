// Detect It Easy: detection rule file
// Author: hypn0 <hypn0@mail.ru>

init("cruncher", "ReXXMaSHeR");

function detect() {
    if (Amiga.compareEP("6100....6000....................................'rMS!'")) {
        sOptions = "by DCS (temp detect)";
        bDetected = true;
    }

    return result();
}