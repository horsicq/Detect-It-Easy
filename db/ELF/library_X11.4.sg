// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://en.wikipedia.org/wiki/X_Window_System
meta("library", "X11");

function detect() {
    if (ELF.isLibraryPresent("libX11.so.6")) {
        bDetected = true;
    }

    return result();
}