// Detect It Easy: detection rule file

init("protector", "Burneye");

function detect() {
    if (ELF.compareEP("FF35........9C608B0D........E9")) {
        sVersion = "1.0";
        bDetected = true;
    }

    return result();
}