// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://en.wikipedia.org/wiki/Argon2
meta("library", "Argon2");

function detect() {
    if (ELF.isLibraryPresent("libargon2.so.1")) {
        bDetected = true;
    }

    return result();
}