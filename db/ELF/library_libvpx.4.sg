// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://en.wikipedia.org/wiki/Libvpx
meta("library", "libvpx");

function detect() {
    if (ELF.isLibraryPresent("libvpx.so.7")) {
        bDetected = true;
    }

    return result();
}