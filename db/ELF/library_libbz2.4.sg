// Detect It Easy: detection rule file
// Author: BJNFNE <bjnfne@web.de>

// https://github.com/libarchive/bzip2
meta("library", "libbz2");

function detect() {
    if (ELF.isLibraryPresent("libbz2.so.1.0")) {
        bDetected = true;
    }

    return result();
}