// Detect It Easy: detection rule file

init("format", "Microsoft Office");

function detect() {
    if (CFBF.getFileFormatVersion() == 3.62) {
        sVersion = "97-2003";
        bDetected = true;
    }

    return result();
}
