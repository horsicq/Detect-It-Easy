// Detect It Easy: detection rule file

meta("format", "Microsoft Office");

function detect() {
    if (CFBF.getFileFormatVersion() == 3.62) {
        sVersion = "1997-2003";
        bDetected = true;
    }

    return result();
}
